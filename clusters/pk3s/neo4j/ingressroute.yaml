apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: neo4j
  namespace: neo4j
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`neo4j.local`)
      services:
        - name: neo4j
          port: tcp-http
