apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Flux will reconcile all resources found in this directory
resources:
  - actual-budget
  - atuin
  - cert-manager
  - cloudflared
  - cv-datastar
  - floci
  - flux-dashboard
  - flux-monitoring
  - forgejo
  - forgejo-runner
  # - hris   # staged, not live: enable once the app publishes its chart to GHCR and the
  #          # credentials are sealed (see clusters/pk3s/hris/kustomization.yaml) — deploy-hris task 6.1
  - media
  - monitoring
  - neo4j
  - nextcloud
  - pangolin
  - papra
  - pve
  - sealed-secrets
  - spec-frontend
  - traefik
  - watcharr
