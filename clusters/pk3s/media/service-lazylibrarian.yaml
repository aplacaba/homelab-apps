apiVersion: v1
kind: Service
metadata:
  name: lazylibrarian
  namespace: media
  labels:
    app: lazylibrarian
spec:
  selector:
    app: lazylibrarian
  ports:
    - name: http
      port: 5299
      targetPort: http
