apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: media-local-path
  resources:
    requests:
      storage: 20Gi
