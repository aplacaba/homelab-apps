apiVersion: v1
kind: Service
metadata:
  name: shelfmark
  namespace: media
  labels:
    app: shelfmark
spec:
  selector:
    app: shelfmark
  ports:
    - name: http
      port: 8084
      targetPort: http
