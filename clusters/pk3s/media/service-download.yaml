apiVersion: v1
kind: Service
metadata:
  name: download
  namespace: media
  labels:
    app: download
spec:
  selector:
    app: download
  ports:
    - name: qbittorrent
      port: 8081
      targetPort: http
    - name: sabnzbd
      port: 8080
      targetPort: http
