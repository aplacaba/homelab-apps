apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-library
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: media-local-path
  resources:
    requests:
      storage: 200Gi
