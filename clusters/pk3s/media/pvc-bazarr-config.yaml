apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bazarr-config
  namespace: media
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: media-local-path
  resources:
    requests:
      storage: 2Gi
