apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  - sealedsecret-cloudflare-api-token.yaml
  # ClusterIssuers re-added in Phase B once cert-manager CRDs are installed
  # - clusterissuer-staging.yaml
  # - clusterissuer-prod.yaml
