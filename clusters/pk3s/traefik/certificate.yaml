apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-watchtoken-org
  namespace: traefik
spec:
  secretName: wildcard-watchtoken-org-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: "*.watchtoken.org"
  dnsNames:
    - "*.watchtoken.org"
