apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirect-to-alacaba
  namespace: cv-datastar
spec:
  redirectRegex:
    regex: ^https://(?:cv\.alacaba\.org|cv\.watchtoken\.org)(.*)$
    replacement: https://alacaba.org/cv$1
    permanent: true
