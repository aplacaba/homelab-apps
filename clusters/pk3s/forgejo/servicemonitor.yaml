apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: forgejo
  namespace: forgejo
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: forgejo
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
