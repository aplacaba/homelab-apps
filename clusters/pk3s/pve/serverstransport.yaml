apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: pve-tls
  namespace: pve
spec:
  # The node certificate is issued for homelab.pve, not the endpoint IP.
  serverName: homelab.pve
  rootCAs:
    - secret: pve-ca
