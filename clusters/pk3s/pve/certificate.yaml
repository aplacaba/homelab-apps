apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pve-local
  namespace: pve
spec:
  secretName: pve-local-tls
  issuerRef:
    name: pve-selfsigned
    kind: Issuer
  dnsNames:
    - pve.local
